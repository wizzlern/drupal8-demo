services:
  # Define a service and mark it as an event subscriber.
  demo_event_subscriber:
    class: Drupal\demo_event\EventSubscriber\DemoEventSubscriber
    tags:
      - { name: event_subscriber }
